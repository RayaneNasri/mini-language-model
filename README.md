# mini-LLM

This mini project bridges the gap between basic neural networks (FFNNs), sequential models (RNNs) and Transformers. By predicting the next character in a sequence of text, i want to build a foundational understanding of the exact mechanism (next-token prediction) that powers modern Large Language Models (LLMs).